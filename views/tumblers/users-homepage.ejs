<!--This is the home page while logged in-->
<%- include('../partials/header') %>

<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
  </head>
  <body>
    
    
    <div>
      <nav>
        <h2 class="title"><%= title %></h2>
        <a class="logout" href="/logout"><h2>Log Out</h2></a>
      </nav>
    </div>

    <br>
    <br>

  <form action="/new-entry">
    <button type="submit" value="New Entry">New Entry</button>
  </form>
  <br>
    
<!--  <table id="list">
      <thead>
          <tr>
              <th>Name</th>
              <th>Age</th>
              <th>Profession</th>
          </tr>
      </thead>
      <tbody>
          <% newEntry.forEach(function(entry) { %>
              <tr>
                <td><%= entry.name %></td>
                <td><%= entry.age %></td>
                <td><a href="//<%= entry._id %>">List of Dates</a></td>
                <% if (new-entry._id.equals(entry._id)) { %>
                  <form action="/new-entry/<%= entry._id %>?_method=DELETE" method="POST" class="secondary-content">
                    <button type="submit" class="btn-small red-text">X</button>
                  </form>
              </tr>
          <% }); %>
      </tbody>
  </table> -->

    <%- include('../partials/footer') %>
